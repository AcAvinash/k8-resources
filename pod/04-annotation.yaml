apiVersion: v1
kind: Pod
metadata:
  name: annotations
  annotations:
    com.project.author: avinash
    com.project.name: expense
spec:
  containers:
  - name: nginx-labels
    image: nginx
    ports:
    - containerPort: 80